/// <reference types="node" />
import BigNumber from "bignumber.js";
import ISerializer from "./ISerializer";
declare type Input = BigNumber | number | string;
declare type JSON = number | string;
export declare class VarUIntSerializer extends ISerializer<BigNumber, Input, JSON> {
    private static validateBN;
    toJSON(value: Input): JSON;
    fromJSON(value: JSON): BigNumber;
    toBuffer(value: Input): Buffer;
    readFromBuffer(buffer: Buffer, offset?: number): {
        res: BigNumber;
        newOffset: number;
    };
}
declare const varuint: VarUIntSerializer;
export default varuint;
