"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
class ISerializer {
    stringify(value) { return JSON.stringify(this.toJSON(value)); }
    parse(value) {
        if (Buffer.isBuffer(value))
            value = value.toString();
        return this.fromJSON(JSON.parse(value));
    }
    fromBuffer(buffer) {
        const { res, newOffset } = this.readFromBuffer(buffer);
        if (newOffset !== buffer.length)
            throw new Error("excess info in the end of buffer");
        return res;
    }
}
exports.default = ISerializer;
