"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const ISerializer_1 = __importDefault(require("./ISerializer"));
class BoolSerializer extends ISerializer_1.default {
    toJSON(value) { return value; }
    fromJSON(value) { return value; }
    toBuffer(value) { return Buffer.from([value ? 1 : 0]); }
    readFromBuffer(buffer, offset = 0) {
        const byte = buffer[offset];
        if (byte === 0)
            return { res: false, newOffset: offset + 1 };
        if (byte === 1)
            return { res: true, newOffset: offset + 1 };
        throw new Error("invalid bool-byte");
    }
}
exports.BoolSerializer = BoolSerializer;
const bool = new BoolSerializer();
exports.default = bool;
